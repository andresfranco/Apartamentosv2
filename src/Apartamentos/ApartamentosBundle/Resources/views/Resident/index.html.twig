{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Resident list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Idnumber</th>
                <th>Name</th>
                <th>Idnumbertype</th>
                <th>Holder</th>
                <th>Email</th>
                <th>Phone</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('resident_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idnumber }}</td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.idnumbertype }}</td>
                <td>{{ entity.holder }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.phone }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('resident_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('resident_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('resident_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
