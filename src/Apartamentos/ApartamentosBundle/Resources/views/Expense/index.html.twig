{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Expense list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Expensedate</th>
                <th>Amount</th>
                <th>Description</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('expense_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.expensedate %}{{ entity.expensedate|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.amount }}</td>
                <td>{{ entity.description }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('expense_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('expense_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('expense_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
