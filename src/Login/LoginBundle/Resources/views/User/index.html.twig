{% extends '::masteradmin.html.twig' %}

{% block contenido %}

    <h3>Users</h3>
    <hr>   
    <div><a href="{{ path('admin_user_new') }}">New User<img src="{{ asset('images/new.png') }}"/></a></div></br>
    <div><a href="{{path('userlist')}}">Listar</a></li></div>            
    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_user_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.username }}</td>
                <td>
                    
                    <div><a href="{{ path('admin_user_show', { 'id': entity.id }) }}"><img src="{{ asset('images/show.png') }}"/></a></div>
                    
               
                </td>
                <td><div><a href="{{ path('admin_user_edit', { 'id': entity.id }) }}"><img src="{{ asset('images/edit.png') }}"/></a></div></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        
 
 {% endblock %}   
